name: DPort Actions Smoke Test

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/actions-smoke.yml

jobs:
  smoke:
    runs-on: macos-26
    steps:
      - name: Show runner
        run: |
          uname -a
          xcodebuild -version
